export const metadata = {
  title: "PlayHub Game Library - Grid View",
  description: "Browse and manage your PlayHub game library",
};

export default function LibraryLayout({ children }) {
  return children;
}
