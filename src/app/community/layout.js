export const metadata = {
  title: "PlayHub Community - Guild Discovery",
  description: "Discover guilds, join communities, and find your next squad on PlayHub",
};

export default function CommunityLayout({ children }) {
  return children;
}
