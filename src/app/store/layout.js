export const metadata = {
  title: "PlayHub Store - Browse Games",
  description: "Browse and buy games on PlayHub. New releases, special offers, and top sellers.",
};

export default function StoreLayout({ children }) {
  return children;
}
